# Codex 重置卡提醒：首次安装

1. 解压安装包到当前用户可写、准备长期保留的目录。不要在安装后移动该目录；Windows 计划任务会引用目录中的脚本。
2. 安装 Windows PowerShell 7、Node.js 24，并通过 npm 全局安装 Codex CLI（`npm install -g @openai/codex`），确保已登录。若要使用飞书通知，另安装本机 `lark-cli`，并在飞书开放平台准备启用了机器人的应用及 App ID、App Secret。
3. 双击 **开始安装.vbs**。点击 **安装本机提醒**，等待窗口显示“本机提醒安装完成”。此步骤会创建桌面快捷方式和当前 Windows 用户的提醒计划任务。
4. 如需飞书私聊，填写应用 App ID、接收人 Open ID（`ou_` 开头）或 Union ID（`on_` 开头），以及 App Secret，点击 **连接飞书**。安装程序会先向该接收人发送一条测试私聊；发送成功后保存配置并锁定输入框。要更换配置，点击 **重新连接** 并在确认框中确认，再填写新信息。机器人须有向接收人发送私聊的权限；使用 Union ID 时，还须能读取用户信息。
5. 本机提醒安装成功后，点击 **完成并打开管理**，程序会进入卡片管理主页面。之后可从桌面快捷方式 **Codex 重置卡提醒** 打开。在 **提醒设置** 中点击 **测试提醒**，核对桌面和飞书是否收到仿真消息。

飞书是可选渠道。即使未连接飞书，只要电脑开机且用户已登录，本地保存的卡片仍可触发 Windows 提醒。若要修复本机提醒或更换飞书应用，重新运行安装目录中的 **开始安装.vbs**；在初始化窗口点击 **重新连接**，二次确认后填写新配置。

安装包不会包含别人的 `config.json`、`.state` 数据库或日志。不要把使用中的整个程序目录直接转给其他用户。
